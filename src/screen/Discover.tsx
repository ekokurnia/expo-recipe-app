import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Discover = () => {
  return (
    <SafeAreaView>
      <Text style={{ fontFamily: "Poppins-Bold", fontSize: 20 }}>Discover</Text>
    </SafeAreaView>
  );
};

export default Discover;
